# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Companion** is an AI-powered French language learning app targeting the TCF (Test de Connaissance du Français) exam. It is a React Native app built with Expo SDK 55, using Expo Router for file-based routing, NativeWind/Tailwind for styling, Supabase for backend, and OpenAI + Azure Speech for AI features.

## Development Commands

```bash
npx expo start          # Start Metro bundler (dev server)
npx expo start --ios    # Start and open iOS simulator
npx expo start --android # Start and open Android emulator
npx expo start --web    # Start web version
```

Linting (ESLint), formatting (Prettier), and pre-commit hooks (Husky + lint-staged) are configured.

```bash
npm run lint            # Run ESLint
npm run format:check    # Check Prettier formatting
npm run type-check      # Run TypeScript type checking
```

## Environment Setup

Copy `.env.example` to `.env.local` and fill in the keys:

- `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_ANON_KEY` — Supabase project (client-bundled)
- `EXPO_PUBLIC_SENTRY_DSN` — Sentry error tracking DSN (client-bundled)

AI API keys (`OPENAI_API_KEY`, `AZURE_SPEECH_KEY`, `AZURE_SPEECH_REGION`) are server-side only — set them via `supabase secrets set`, never in the client.

## Architecture

**TCF spec source of truth:** the app targets **TCF Canada** (verified 2026-05-07; expanded + re-verified 2026-05-10 by story 10-1). Section question counts and time limits are pinned in `src/lib/constants.ts` (`TCF` object). The full spec — scoring scale + IRCC CLB equivalency table + per-CEFR passage expectations + Expression Orale 4-criterion rubric (FEI examiner convention; not published verbatim by the publisher) + Expression Écrite evaluation criteria + vocabulary frequency tiers + linguistic accuracy references — is documented at `docs/tcf-spec-source.md` (11 sections). Every TCF-derived value in the codebase appears in `docs/tcf-spec-citations.md` (the citations matrix); adding a new TCF claim without a matrix row fails `src/lib/__tests__/tcf-spec.test.ts` (12 cases). Four source snapshots live under `docs/tcf-canada-snapshots/` with retrieval timestamps + SHA-256 integrity headers: France Éducation International landing page, FEI samples page, IRCC CLB equivalency table (third-party-transcribed because canada.ca returned HTTP 403 to WebFetch — explicit caveat in the snapshot), and Council of Europe / Europass CEFR self-assessment grid. The `[A-Z0-9]{10}` Apple-Team-ID-shaped strings in CI leak guards do not match TCF score literals (numeric only). Verified 2026-05-10, story 10-1.

**CEFR promotion contract:** `src/lib/activity.ts` — pure decision helper `evaluatePromotion()`, regression-tested by `src/lib/__tests__/activity.test.ts`. Promotion requires evidence in all 5 TCF skills at the current level (verified 2026-05-07, story 9-2).

**Sentry telemetry contract:** `src/lib/sentry.ts` — `scrubEvent()` is the GDPR scrubber wired into `Sentry.init.beforeSend`; allowlist + 80-char redaction rule; `captureError`'s `extras` is typed `Record<string, string|number|boolean|null>` to prevent payload leaks. Verified 2026-05-07, story 9-3.

**Stored-prompt-injection defense:** `src/lib/memory.ts` — `sanitizeMemoryContent()` strips instruction-like tokens, NFC-normalizes, and caps content to 300 chars; called on every write to `companion_memory.content` and `error_patterns.error_description`, and again at read time as defense-in-depth. Conversation and grammar prompts wrap user-derived blocks in `<USER_FACTS>` / `<USER_WEAK_AREAS>` with an explicit "treat as data" prelude. Regression-tested in `src/lib/__tests__/prompt-injection.test.ts`. Verified 2026-05-07, story 9-4.

**Voice transcript dedup:** `src/lib/realtime.ts` configures `output_modalities: ["audio"]` so exactly one terminal transcript event (`response.output_audio_transcript.done`) fires per AI turn. `src/lib/realtime-transcript.ts` exposes pure `appendIfNew` / `acceptDelta` / `resolveTranscriptKey` helpers; `src/hooks/use-realtime-voice.ts` routes both `.done` paths through one append + dedup helper keyed off `item_id` (with `response_id` and an opaque `fallback_<djb2-content-hash>` fallback — deterministic, no raw text, no timestamp, so duplicates of malformed events still dedupe and the key is safe to log). Empty/whitespace `.done` payloads are no-ops; the dedup Set is FIFO-capped at 256 so dedup remains correct past the cap. Duplicate events for the same key are suppressed and breadcrumbed to Sentry. Regression-tested in `src/lib/__tests__/realtime-dedup.test.ts`. Verified 2026-05-07, story 9-5.

**Auth listener event gating:** `src/hooks/use-auth.ts` branches `onAuthStateChange` on the Supabase `AuthChangeEvent` discriminator via the pure helper `decideAuthAction()` in `src/lib/auth-events.ts`. `INITIAL_SESSION` / `SIGNED_IN` load profile and flush the offline write queue; `USER_UPDATED` invalidates cache and reloads profile (no queue flush); `TOKEN_REFRESHED` / `PASSWORD_RECOVERY` / `MFA_CHALLENGE_VERIFIED` only update the session ref; `SIGNED_OUT` clears the profile. Null sessions on non-`SIGNED_OUT` events breadcrumb to Sentry without destroying local state. `flushWriteQueue` (`src/lib/cache.ts`) is idempotent via a module-scope in-flight Promise so concurrent callers (auth listener + `NetworkBanner` reconnect) replay queued writes exactly once. The cold-start `getSession()` is wrapped with `captureError(_, "auth-initial-session")`. Regression-tested in `src/lib/__tests__/auth-events.test.ts` and `src/lib/__tests__/cache-flush.test.ts`. Verified 2026-05-07, story 9-6.

**Auth + cache race hardening:** `src/hooks/use-auth.ts` `loadProfile` guards `setProfile` and `flushWriteQueue` with a `useAuthStore.getState().user?.id === userId` check (via the pure helper `applyProfileIfFresh`) so an in-flight load that resolves after `SIGNED_OUT` does not clobber the cleared profile (drops result + breadcrumb with `phase: "load-profile-stale"`). `src/lib/cache.ts` `flushWriteQueue` merges any writes added to the queue between the snapshot read and the post-flush persist (atomic `persistQueue([...remaining, ...newWrites])`) so an `enqueueWrite` mid-flight is preserved. The IIFE wraps its body in `try/catch` and resolves to `0` on internal errors (e.g. `isOnline` throwing), preserving the in-flight Promise contract for concurrent callers; failures emit `captureError(_, "cache-flush-internal")`. `src/store/auth-store.ts` exposes a `profileFetchFailed` flag set by `loadProfile`'s catch path; the auth guard at `app/_layout.tsx` reads the flag and routes to a `ProfileRetryScreen` retry surface (with `retryProfileFetch`) instead of `/onboarding` when both network and cache reads fail. Regression-tested in `src/lib/__tests__/auth-load-profile-stale.test.ts`, `src/lib/__tests__/cache-flush.test.ts` (Cases 16–18), `src/lib/__tests__/profile-fetch-failed-flag.test.ts`. Verified 2026-05-07, story 9-10.

**AI response validation:** `src/lib/openai.ts` `chatCompletionJSON<T>` requires a Zod schema (`z.ZodType<T>`) and a `feature` tag. On schema parse failure the call is retried once with a fresh model invocation; if the retry also fails, `captureError(_, "ai-schema-parse-failed", { feature, attempt: 2, code })` fires and the call rejects. All AI response schemas live in `src/lib/schemas/ai-responses.ts` — one schema per call site (listening / reading / grammar / writing-prompt-gen / writing-evaluation / dictation / echo / translation-gen / translation-evaluation / fact-extraction / micro-drill / error-pattern-batch / mock-test-section / pronunciation / placement-test / conversation-feedback) plus the common atomics `mcqOptionSchema` (4-option enforcer) and `mcqQuestionSchema` (4-options-with-1-correct via `superRefine`). Hand-rolled validators (`validateMCQExercise`, `validateEchoResponse`, `validateTranslationResponse`, `validateEvaluationResponse`, the inline mock-test option filter, the placement-test polymorphic-options normalizer at `placement-test.tsx:489-518`) are deleted; the schemas express the same rules declaratively (`mcqQuestionSchema.superRefine` for the 4-options + 1-correct invariant, `placementTestSchema.preprocess` for the polymorphic-options normalization including object-vs-array options and `correct_answer` resolution). Sentry never sees the offending response text — only the `ZodIssueCode`, the issue path, and the call-site `feature` tag (allowlist-safe per `src/lib/sentry.ts:25`). The placement test uses `parseRetries: 2` (default elsewhere is 1) to reflect its higher stakes. Inferred types (`WritingEvaluation`, `ConversationFeedback`, `EchoSentence`, `TranslationSentence`, `TranslationEvaluation`, `WritingError`, `TranslationDimensionScore`) are now `z.infer<typeof X>` in `src/types/exercise.ts` and `src/types/conversation.ts`, so the type system mirrors runtime validation exactly. Regression-tested in `src/lib/schemas/__tests__/ai-responses.test.ts` and `src/lib/__tests__/chat-completion-json.test.ts`. Verified 2026-05-08, story 9-7.

**Speaking section pipeline:** `app/(tabs)/mock-test/speaking.tsx` runs the TCF Canada Expression Orale 3-task assessment as a record-and-grade flow (no Realtime — Realtime examiner role-play is Epic 10.6). Per task: `useAudioRecorder(RECORDING_OPTIONS_LOW_BITRATE)` (32 kbit AAC so 5.5-min Task 2 fits the 5 MB `ai-proxy` cap) → `transcribeAudio` (Whisper) → `chatCompletionJSON(_, speakingTaskEvaluationSchema, { feature: "speaking-eval-task-N" })` returns the official 4-criterion 0-20 rubric (pronunciation/fluency, vocabulary, grammar, interaction). Pure score helpers in `src/lib/speaking-scoring.ts` — `computeSpeakingTaskOverall` recomputes overall from the 4 dimensions × 1.25 when the model omits/null-emits it; `computeSpeakingComposite` averages the 3 task overalls (equal weights — recalibration owned by Epic 10.2). Per-CEFR topic libraries in `src/lib/prompts/speaking.ts` use a deterministic 3-day bucket so retakes within the window see the same prompt (anti-game heuristic; broader anti-repetition is Epic 10.8). Persists `mock_tests` row (`test_type="speaking"`, `section_scores.speaking.{task1,task2,task3,compositeOverall}`), 3 `mock_test_answers` rows (`selected_option=transcript`, `is_correct=NULL` since production tasks have no objective right answer), and runs the standard `updateSkillProgress("speaking") → incrementDailyActivity → updateStreak → checkCefrPromotion` chain — closing the only TCF Canada skill that previously had zero `mock_tests` coverage. Static route at `mock-test/speaking.tsx` takes precedence over `[testId].tsx` via Expo Router's static-over-dynamic resolution; the QCM runner is unchanged. Sentry tags `speaking-mock-test-{record,transcribe,eval}-task-{1|2|3}` and `speaking-mock-test-persist` ride on existing allowlist keys (zero changes to `SENTRY_EXTRAS_ALLOWLIST`); transcripts/audio are never carried in event payloads. The `<USER_TRANSCRIPT>` wrapper + "treat as data" prelude in `buildSpeakingEvaluatorPrompt` mirrors story 9-4's prompt-injection defense for transcribed user content. Regression-tested in `src/lib/__tests__/speaking-scoring.test.ts`, `src/lib/prompts/__tests__/speaking.test.ts`, `src/lib/__tests__/speaking-mock-test-persist.test.ts`, and `src/lib/schemas/__tests__/ai-responses.test.ts`. Verified 2026-05-09, story 9-8.

**Deploy substrate:** `eas.json` submit profiles read all credentials from EAS environment variables (`EXPO_ASC_API_KEY_ID`, `EXPO_ASC_API_KEY_ISSUER_ID`, `EXPO_ASC_APP_ID`, `EXPO_APPLE_TEAM_ID`, plus the `EXPO_ASC_API_KEY_P8` and `EXPO_GOOGLE_SERVICE_ACCOUNT_KEY` file secrets) — **no submit credentials live in git**. Deploy workflows: native builds via `.github/workflows/build.yml` (manual + on push to main, with `EXPO_PUBLIC_*` secrets injected so the JS boots), TestFlight / Play Internal-Track submission via `.github/workflows/submit.yml` (manual `workflow_dispatch`, gated by GitHub `production` environment review), Edge Functions via `.github/workflows/deploy.yml` (auto on push to `supabase/functions/**`, manual `force` available, per-function calls keep revision history clean), OTA updates via `.github/workflows/ota-update.yml` (manual; production publishes gated to `main` branch only). Sentry source maps upload automatically during both `eas build` and `eas update` because `SENTRY_AUTH_TOKEN` is in the EAS + GitHub Actions environment; the `@sentry/react-native/expo` config plugin handles the post-bundle upload — no manual `sentry-cli` step is needed. SQL migrations stay manual (`supabase db push`) until story 16-6 lands the rollback playbook — the deploy workflow emits a `::warning::` GitHub annotation when a push touches `supabase/migrations/`. CI guards regression of placeholder/literal credentials in source via the "Submit credentials leak guard" step in `ci.yml` (Apple Team ID `[A-Z0-9]{10}` and ASC App ID `[0-9]{10}` patterns are scoped to JSON `appleTeamId` / `ascAppId` keys to avoid false positives; placeholder pattern uses `[_]` character-class obfuscation so the regex source does not self-match). `.gitignore` blocks `google-service-account.json` (no `*.json` blanket rule exists — explicit line is the only safe defense) and was narrowed from `_bmad*` (which silently dropped new story files) to `_bmad/` + `_bmad-output/*` with `!_bmad-output/{implementation,planning}-artifacts/` carve-outs. Operator runbook: `_bmad-output/planning-artifacts/runbooks/submit-and-deploy.md`; OTA-specific rollback: `_bmad-output/planning-artifacts/runbooks/ota-hotfix.md`. Verified 2026-05-09, story 9-9.

**TCF scoring pipeline (per-skill, publisher-anchored):** post-Epic-10.2, scoring is split into per-skill conversion functions in `src/lib/scoring.ts`: `rawPercentToListeningReadingScore(rawPercent, "listening" | "reading")` returns a 0–699 TCF score for the QCM skills; `rawPercentToWritingSpeakingScore(rawPercent)` returns a 0–20 publisher score for the production-task skills. Both use `[lower, upper)` raw-% boundary semantics so exact-boundary inputs land in the higher CLB band. Band boundaries are anchored to the IRCC CLB equivalency table at `src/lib/ircc-bands.ts` `IRCC_CLB_BANDS` (sourced verbatim from `docs/tcf-spec-source.md §2.2`; CLB 7 is the Express Entry threshold). The legacy 7-band linear `rawToTCFScore` is **deleted**, not aliased — every caller is updated. `calculateCompositeScore` was renamed to `calculateInternalCompositeForUI`, JSDoc-flagged as NOT IRCC-equivalent and NOT used by the promotion engine, **and scoped to Listening + Reading only** — Writing/Speaking (0–20 scale) and Grammar (not TCF Canada) are silently dropped to avoid mixed-scale averaging. `SKILL_WEIGHTS_TCF_CANADA` (4-skill, equal 0.25 weights for listening/reading/writing/speaking) replaces the prior 5-skill `SKILL_WEIGHTS`; Grammar stays in the `TCFSkill` union as a non-TCF practice skill per operator decision (`docs/tcf-spec-source.md §10` follow-up #1). Speaking pipeline (`src/lib/speaking-mock-test-persist.ts`) now persists `mock_tests.total_score` in publisher-scale 0–20 for `test_type="speaking"` via the new `computeSpeakingScore0to20` helper in `src/lib/speaking-scoring.ts` (`composite / 5`); pre-10-2 historical rows hold legacy 0–699 values (discontinuity documented but not backfilled — Epic 17.1 owns schema versioning). `cefrLevelFromWritingSpeakingScore` maps 0–20 → CEFR via CLB equivalence (CLB 1–3 → A1, CLB 4 → A2, CLB 5–6 → B1, CLB 7–8 → B2, CLB 9 + lower CLB 10–12 → C1, upper CLB 10–12 → C2 — operator-derived split since the IRCC table groups CLB 10–12 as one bucket). `formatWritingSpeakingScore` provides the parallel UI formatter for 0–20 alongside `formatTCFScore` for 0–699. `WritingSpeakingScore` and `TCFScore` type aliases are documentation-only naming conventions — both alias `number` and the compiler does NOT enforce scale separation; rely on function names + JSDoc at call sites. UI-labeling `CEFR_LEVELS.tcfScoreMin/Max` round-number bands stay for the self-assessment-grid display; IRCC math uses `IRCC_CLB_BANDS`. `app/(tabs)/mock-test/results.tsx:60` literal `500` replaced with `TCF.C1_MIN` (`src/lib/constants.ts` JSDoc explains the UI-rounding intent). The promotion engine (`src/lib/activity.ts` `evaluatePromotion`, story 9-2 contract) still uses the internal 0–100 `skill_progress.score` scale (intentional UX-soft semantics; switching to IRCC bands is a deferred follow-up). Regression-tested in `src/lib/__tests__/ircc-bands.test.ts`, `scoring.test.ts`, `speaking-scoring.test.ts`, `speaking-mock-test-persist.test.ts`, plus the Story 10-2 "promotion does not consume composite" block in `activity.test.ts` (now includes a HIGH-composite-FEW-passing vs LOW-composite-MANY-passing assertion that directly demonstrates per-skill semantics). Verified 2026-05-10, story 10-2.

**TCF per-CEFR passage calibration:** post-Epic-10.3, the listening / reading / writing prompt builders (`src/lib/prompts/{listening,reading,writing}.ts`) carry per-CEFR word ranges anchored to `docs/tcf-spec-source.md §3.1` (listening — operator-derived, Beacco-cross-checked), `§4.1` (reading — same), and `§5.1` (writing — **publisher-verbatim, enforcement-grade per §5.3**). Listening A1 widened from 30-50 to 30-80; A2 60-150; B1 100-200; B2 150-300; C1 250-500; C2 350-600 (was 250-350) — addresses audit P1-3 ("A1 too long, B2/C1 too short"). Reading B1 120-250; B2 250-450 (was 200-300); C1 450-700 (was 300-400); C2 600-900+ (was 350-500); A1/A2 unchanged — addresses audit P1-3 ("B2/C1 way too short"). Writing Task 1 corrected to 60-120 (was 50-80) and Task 3 corrected to 120-180 (was 250-300, near-doubled-the-cap) per publisher-verbatim §5.1; Task 2 unchanged at 120-150. The §5.3 disqualification rule ("A submission outside these word counts is automatically evaluated as 'A1 non atteint' regardless of content quality") is now surfaced at the top of `buildWritingEvaluatorPrompt` so the AI never generates prompts demanding more text than the publisher allows. New `writingTaskWordRange(taskNumber)` helper exported from `src/lib/prompts/writing.ts` is the single source of truth for the per-task ranges; `src/hooks/use-exercise.ts` writing flow imports it (collapsing the pre-10-3 three-site lockstep risk). UI auto-propagation: `app/(tabs)/practice/writing.tsx` consumes `writingPrompt.{minWords,maxWords}` from state and reflects new ranges with no UI code change. `src/lib/prompts/mock-test.ts` had its stale `## Scoring Calibration` linear-band block deleted (Story 10-2 already deleted `rawToTCFScore`) and its inline `wordCount: 150 / 200` placeholders replaced with per-difficulty references to §4.1 — the AI generates content, not scores. Per-CEFR ranges deliberately overlap (per §3.1 / §4.1 note: length is not the CEFR diagnostic — syntactic density + lexical frequency + abstract/concrete ratio differentiate levels). Regression-tested in `src/lib/prompts/__tests__/passage-calibration.test.ts` (positive + negative substring assertions for all 6+6+3 levels/tasks + the §5.3 surface check + `writingTaskWordRange` helper round-trip). Verified 2026-05-10, story 10-3.

**TCF per-CEFR vocabulary tiers:** post-Epic-10.4 (Phase 1), every CEFR-aware prompt builder in `src/lib/prompts/{listening,reading,writing,conversation,echo,translation,speaking,mock-test}.ts` carries a Vocabulary Constraint block sourced from `src/lib/prompts/vocabulary-tiers.ts`. The new module exports `vocabularyTier(cefrLevel)` returning `{ approxWordCap, exemplars, forbiddenLowerTier }` per `docs/tcf-spec-source.md §7.2` (operator-derived heuristic caps: A1 ~700 / A2 ~1700 / B1 ~2800 / B2 ~5000 / C1 ~7500 / C2 ~10000) plus 10–20 hand-curated high-frequency exemplars per level (Wiktionary "Liste des 1000 mots les plus fréquents en français" CC-BY-SA + DGLF / Service-Public.gouv.fr usage frequency) plus a forbidden-lower-tier list per `docs/tcf-spec-source.md §8.1` (A1 must not contain `cependant` / `néanmoins` / `force est de constater`; A2 must not contain `néanmoins` / `force est de constater`; B1 must not contain `force est de constater` / `il n'en demeure pas moins`; B2 must not contain `force est de constater` / `idiosyncrasie` / `palimpseste`; C1+ no forbidden list — full upper register expected). `buildVocabularyConstraintBlock(cefrLevel)` renders the markdown block dropped into single-level builders (listening / reading / writing / conversation / echo / translation generator + evaluator / speaking evaluator); `buildAggregatedVocabularyConstraintTable()` renders a one-row-per-level compact table for `mock-test.ts` (which spans A1–C2 in a single section). `buildSpeakingTaskPrompt` is intentionally NOT integrated — it returns user-facing UI chrome (English instruction + French topic), not an AI prompt. Helper throws on non-CEFR runtime input (Story 10-3 `writingTaskWordRange` defensive pattern). **Phase 2 (Beacco-verbatim word lists from Didier 2007–2011) deferred** — operator-action per `docs/tcf-spec-source.md §10b` item #5 (~€120–€180 acquisition cost). Story 9-4's stored-prompt-injection defense holds: the constraint block is statically built from a controlled module — no user input flows through it; deterministic output verified by test. Regression-tested in `src/lib/prompts/__tests__/vocabulary-tiers.test.ts` (40 helper-contract cases — caps, monotonicity, forbidden-token, determinism, runtime throw) and `vocabulary-integration.test.ts` (53 surface checks: 8 builders × 6 levels via `it.each` + mock-test aggregated table + forbidden-token regression guards). Verified 2026-05-10, story 10-4.

**TCF placement-test prompt extraction:** post-Epic-10.5, the placement-test system prompt is the pure function `buildPlacementTestPrompt()` at `src/lib/prompts/placement.ts` — the 145-line inline `SYSTEM_PROMPT` const previously at `app/onboarding/placement-test.tsx:75-145` is **deleted** (Story 10-2 "delete don't alias" pattern). Per-CEFR vocabulary tiers are sourced from `src/lib/prompts/vocabulary-tiers.ts` `buildAggregatedVocabularyConstraintTable()` (Story 10-4) — because the placement test spans A1–C2 in a single AI call (same pattern as `mock-test.ts`); the inline `top-500` / `top-1000` / `top-3000` / `top-5000` strings that previously hard-coded vocab tiers were deleted to eliminate silent drift from `vocabulary-tiers.ts`. Question-distribution metadata is centralised at `PLACEMENT_LEVEL_RANGES` (A1:3 / A2:3 / B1:3 / B2:3 / C1:2 / C2:1 = 15 total) and `TOTAL_PLACEMENT_QUESTIONS`, both exported from the new module and consumed by both the prompt builder AND the scoring path (`levelForQuestion`, `calculateLevel`, `buildResultsSummary` in `placement-test.tsx`). The builder accepts no parameters (the placement test is a fixed pre-onboarding artifact); output is deterministic (Story 9-4 contract) and regression-tested by `src/lib/prompts/__tests__/placement.test.ts` (helper-contract + substring + negative-assertion + determinism cases) plus the extended `vocabulary-integration.test.ts` (Story 10-4 review patch P9 cross-check matrix extended from 4 to 5 builders; placement-row guidance is asserted free of forbidden tokens at each CEFR level by extracting just the per-level guidance row, because the aggregated A1–C2 prompt legitimately surfaces C1-tier tokens like `force est de constater` in the C1 competencies row). Citations matrix §9 row extends from 8 to 9 builders; `docs/tcf-spec-source.md §10` follow-up #7 closes (the optional post-placement UI indicator is deferred to Epic 14.X). Schema unchanged — `placementQuestionSchema` + `placementTestSchema` exported from `src/lib/schemas/ai-responses.ts` remain the runtime contract (Story 9-7). User-facing behavior unchanged: same 15 questions, same distribution, same `gpt-4o` / temp `0.5` / `maxTokens 4096` / `parseRetries 2`. Verified 2026-05-10, story 10-5.

**TCF Expression Orale 5-dimension rubric:** post-Epic-10.6, the speaking-task evaluator at `src/lib/prompts/speaking.ts` `buildSpeakingEvaluatorPrompt` scores against **5 criteria** instead of the Story 9-8 4-criterion convention: `pronunciationFluencyScore` + `vocabularyScore` + `grammarScore` + `interactionScore` + **`sociolinguisticScore`** (NEW — adéquation à la situation de communication per `docs/tcf-spec-source.md §6.3` publisher categorization; assesses register appropriateness, situational code-switching, formality calibration, and CEFR-tier sociolinguistic markers). Each dimension is 0-20; sum is 0-100; `RUBRIC_TO_COMPOSITE` in `src/lib/speaking-scoring.ts` updated from `1.25` (4-dim) to `1.0` (5-dim) via the derived form `100 / (5 × 20)`. `computeSpeakingTaskOverall` recompute factors all 5 dimensions when the model omits `overallScore`. `computeSpeakingComposite` (3-task average) and `computeSpeakingScore0to20` (`composite / 5` for IRCC publisher scale) are dimension-agnostic and unchanged. Schema enforcement: `speakingTaskEvaluationSchema` at `src/lib/schemas/ai-responses.ts` requires `sociolinguisticScore` (NOT optional) — a 4-dimension legacy AI response fails Zod parse and triggers Story 9-7's retry-once path. The evaluator prompt also gains a Task 2 prep-window instruction (conditional on `taskNumber === 2`) telling the AI not to penalize silence during the publisher's 2-minute preparation window — partial §6.1 closure; the full Realtime UI gating + examiner role-play (§6.4) deferred to Epic 10.X follow-up (filed as §10 follow-up #10). Persistence forward-only: pre-10-6 `mock_tests.section_scores.speaking.task{1,2,3}` rows hold 4 dimensions; post-10-6 hold 5 (`section_scores` is JSONB so the additive field grows without migration). The full 9-criterion publisher breakdown (3 categories × 3 sub-criteria) requires operator-fetch _Manuel du candidat TCF_ (`docs/tcf-spec-source.md §10b` item #2); deferred to Phase-2. Story 9-4 prompt-injection defense (`<USER_TRANSCRIPT>` wrapper + "treat as data" prelude) and Story 10-4 vocab-tier integration (`buildVocabularyConstraintBlock(cefrLevel)`) both hold unchanged. Verified-correct Story 9-8 surfaces NOT touched: per-task durations (Task 1: 120s / Task 2: 330s / Task 3: 270s), `buildSpeakingTaskPrompt` per-CEFR topic libraries, deterministic-3-day-bucket selector, `MAX_TRANSCRIPT_CHARS` cap. Regression-tested in `src/lib/__tests__/speaking-scoring.test.ts` (constant pin + 5-dim recompute + sociolinguistic-pull-down case + end-to-end 5-dim → publisher-scale routing), `src/lib/prompts/__tests__/speaking.test.ts` (Section 5 + composite-formula × 1.0 substring assertions parameterized over 6 levels × 3 tasks + Task 2 prep-window conditional + negative assertion against legacy × 1.25 multiplier), and `src/lib/schemas/__tests__/ai-responses.test.ts` (`sociolinguisticScore` required-field + boundary cases). Verified 2026-05-10, story 10-6.

**TCF linguistic accuracy pass:** post-Epic-10.7, four `docs/tcf-spec-citations.md §8` linguistic-accuracy ✗ DELTA rows flip to ✓ Verified. (a) **§8.4 voice-mode emoji + markdown drop.** `src/lib/prompts/conversation.ts` `buildConversationPrompt` Correction Report block — emoji (📝 / 💡 / ✅), markdown bold (`**Corrections:**`), and horizontal rules (`---`) removed; the regex-extractable correction-line shape `"User said" → "Correct form" (explanation)` is preserved so `parseCorrections` at `src/hooks/use-realtime-voice.ts:152-171` continues to extract corrections via `/"([^"]+)"\s*→\s*"([^"]+)"\s*\(([^)]+)\)/g`. The new prompt explicitly tells the model that responses are read aloud verbatim by TTS and instructs no markdown / no emoji. Architectural successor — `report_correction` tool-call replacing the regex parser — owned by Epic 11.1. (b) **§8.1 "Force est de constater" reclassified as locution verbale figée.** `conversation.ts` debate-mode "advanced connectors" list split into three correctly-classified sub-categories: **Connecteurs** (Cependant, Néanmoins, Toutefois, En revanche, D'une part... d'autre part), **Locutions verbales figées** (Force est de constater que, Il faut admettre que, Il n'en demeure pas moins que, Quoi qu'il en soit, À supposer que), **Déclencheurs du subjonctif** (Bien que (+ subjonctif), Quand bien même). The same misclassification echo at `src/lib/prompts/writing.ts` "Expected connectors by level" is rebranded to "Expected discourse markers (connectors + fixed expressions) by level" and `src/lib/prompts/placement.ts` C1 competencies row gains inline `[connector]` / `[fixed expression]` labels + accent-orthography fixes (`passé simple`, `en dépit de`); `src/lib/prompts/echo.ts:39` (C1 example structure) and `src/lib/prompts/vocabulary-tiers.ts` (C1+ forbidden-lower-tier token + C2 exemplar from Story 10-4) verified-correct, not touched. (c) **§8.2 Alliance Française CEFR-label convention.** `src/types/cefr.ts` `CEFR_LEVELS` `nameFr` fields rewritten uniformly: A1 "Élémentaire 1", A2 "Élémentaire 2" (was the audit-flagged "Élémentaire avancé"), B1 "Intermédiaire 1", B2 "Intermédiaire 2", C1 "Avancé 1", C2 "Avancé 2" (was "Maîtrise" — an Eduscol parenthetical mixed in). JSDoc documents the convention + the Service-Public 3-tier alternative-rejected rationale (A1 + A2 would collapse to identical `nameFr` "Élémentaire" / "Élémentaire"); profile screen consumer (`app/(tabs)/profile/index.tsx`) re-renders the new labels automatically. (d) **§8.3 Québécois drop.** The `quebecois` arm of `DIALECT_GUIDANCE` and the `"quebecois"` member of the `dialect?` union in `buildListeningExercisePrompt` (`src/lib/prompts/listening.ts`) are **removed** per audit decision D5 (`shippable-roadmap.md §6`) — Québécois variant deferred to v2 with native-speaker review (OQLF Banque de dépannage linguistique conformance + accurate IPA for /t/ → [ts] affrication + real markers: `icitte` / `pantoute` / `astheure` / `piasse` / `char` / `magasiner`); the single caller (`src/hooks/use-exercise.ts:120`) already passes `"metropolitan"`. A pinned `@ts-expect-error` test guard at `src/lib/prompts/__tests__/listening.test.ts` fails loudly if a future patch widens the union back. Story 9-4 stored-prompt-injection defense (`<USER_FACTS>` / `<USER_WEAK_AREAS>` wrappers + "treat as data" prelude) and Story 9-5 voice transcript dedup (`output_modalities: ["audio"]` + `appendIfNew` dedup) hold unchanged. Regression-tested in `src/lib/prompts/__tests__/conversation.test.ts` (NEW — emoji-guard + HR-guard + parseCorrections-regex compatibility + debate-mode 3-category split + Story 9-4 wrapper invariants regression), `src/types/__tests__/cefr.test.ts` (NEW — 6 `nameFr` cases + uniform-convention assertion + negative against deleted labels), `src/lib/prompts/__tests__/listening.test.ts` (NEW — `@ts-expect-error` type-narrowing guard + Québécois-token leakage guard), `src/lib/prompts/__tests__/writing.test.ts` (NEW — discourse-markers framing + C1-C2 `force est de constater` still-present + Story 10-4 per-level filter regression), and `src/lib/prompts/__tests__/placement.test.ts` (EXTENDED — fixed-expression label + accent-orthography fixes + negative against pre-10-7 framing). Verified 2026-05-10, story 10-7.

**TCF exercise anti-repetition:** post-Epic-10.8, `src/hooks/use-exercise.ts` `generateExercise` wraps each of the 4 practice-skill branches (listening / reading / grammar / writing) with a fetch-seen → generate → filter-unseen → retry-once-on-too-few-questions pipeline that prevents the same AI-generated MCQ stem from appearing twice in a user's last 100 completed exercises at the same `(skill, cefr_level)` tuple. The orchestration is encapsulated in two pure pipeline helpers in `src/lib/exercise-dedup.ts`: `runMcqDedupPipeline` (for listening / reading / grammar — generate → filter → retry-at-temp-0.6-if-too-few-fresh → fall-back-on-exhaustion) and `runWritingDedupPipeline` (for writing — generate → regenerate-once-if-seen → accept-on-still-seen). The supporting pure helpers are `normalizeQuestionStem` (NFC + locale-fr lowercase + trim + whitespace-collapse), `hashQuestionStem` (djb2 over normalized stem), `extractExerciseHashes` (pulls stems from MCQ skills + writing prompt), `filterUnseenQuestions`, and `isWritingPromptSeen`; the per-skill threshold `MIN_FRESH_QUESTIONS_PER_SKILL` (4 for MCQ skills, 1 for writing) is exported from the same module. The Supabase I/O layer at `src/lib/exercise-dedup-db.ts` `getSeenHashes(userId, skill, cefrLevel, opts?)` reads the user's last 100 completed exercises matching the tuple — on Supabase error it returns an empty set + `captureError(_, "exercise-dedup-fetch")` so generation proceeds unfiltered (Story 9-10 resilience pattern; never block the user on a dedup query failure). The shared djb2 helper is extracted from `src/lib/realtime-transcript.ts` (Story 9-5) into a new `src/lib/text-hash.ts` module — Story 9-5's `fallback_<hash>` keys and Story 10-8's `question_stem_hashes` column now use byte-identical hashes from a single source of truth (Story 10-2 "delete don't alias" pattern; the private `hashText` is deleted and re-imported, not legacy-aliased). Forward-only schema migration `supabase/migrations/20260511000000_exercise_question_stem_hashes.sql` adds `question_stem_hashes TEXT[]` to `exercises` + a GIN index reserved for Epic 13.x server-side item bank caching; pre-10-8 rows stay NULL and the dedup helper treats NULL as "no hashes." `persistExercise` insert payload includes `question_stem_hashes: extractExerciseHashes(state.exercise)` so the seen-set grows monotonically as users complete exercises; the offline-write queue auto-includes the new column without cache-layer changes. On retry-exhausted (still too few fresh questions after the retry), the unfiltered result is accepted and `captureError(new Error("exercise-dedup-exhausted"), "exercise-dedup-exhausted", { skill, cefrLevel, generatedCount, filteredCount, seenCount, retries })` fires so the operator can see the AI diversity ceiling in prod — Sentry allowlist (`src/lib/sentry.ts` `SENTRY_EXTRAS_ALLOWLIST`) extended with `generatedCount` / `filteredCount` / `seenCount` / `retries` keys (all small bounded integers; Story 9-3 telemetry-allowlist contract preserved). Retry calls use `temperature: 0.6` (more diversity) and `feature: "exercise-<skill>-retry"` so Sentry can distinguish first-try vs retry parse failures from Story 9-7's retry-once-on-schema-parse-failure path. **Speaking-task dedup is NOT touched** — Story 9-8's deterministic 3-day-bucket selector at `src/lib/prompts/speaking.ts` `computeTopicBucket` is the speaking anti-repetition contract; the two contracts coexist (practice-skill dedupe is per-user-hash-set, speaking dedupe is deterministic-per-bucket). Mock-test section dedupe + cross-user item sharing + vector-embedding-based similarity dedupe + pronunciation / dictation / echo / translation dedupe are all out of scope (Epic 13.x performance / Epic 17.x mock-test caching / Epic 11.6 embedding-based dedup / future sub-stories). Stories 9-1 through 10-7 invariants all hold unchanged. Regression-tested in `src/lib/__tests__/text-hash.test.ts` (NEW — 7 djb2 cases including a `hashText("hello world") = "eslcxt"` sentinel pin and a collision-resistance smoke), `src/lib/__tests__/exercise-dedup.test.ts` (NEW — pure-helper coverage of `normalizeQuestionStem` idempotence + NFC invariance, `hashQuestionStem` case + whitespace insensitivity, `extractExerciseHashes` 4-skill shape + defensive null/empty handling, `filterUnseenQuestions` identity / partial / full overlap, `isWritingPromptSeen` true/false/empty/case branches, `MIN_FRESH_QUESTIONS_PER_SKILL` exhaustiveness, plus full `runMcqDedupPipeline` + `runWritingDedupPipeline` orchestration cases for empty-seen / partial-overlap-retry-fresh / partial-overlap-retry-worse / full-overlap-exhausted / below-threshold-no-retry / writing-fresh / writing-retry-fresh / writing-retry-exhausted), `src/lib/__tests__/exercise-dedup-db.test.ts` (NEW — happy-path 15-hash union + cross-row dedup + empty / null data + Supabase-error resilience + throw resilience + NULL column / non-array defensive guards + per-column `.eq()` filter pinning + `.limit(N)` value pinning + `orderBy completed_at DESC` pinning + writing-skill `exercise_type = "free_write"` mapping), and `src/hooks/__tests__/use-exercise.test.ts` (NEW — `persistExercise` insert payload contract: 5-hash listening + 1-hash writing + 0-hash dedup-exhausted writing branch via the `dedupExhausted` flag gate). Verified 2026-05-10, story 10-8.

**Realtime correction tool-call protocol:** post-Epic-11.1, the Story 10-7 minimum-viable bridge (strip emoji + markdown decoration from the Correction Report block; preserve the `parseCorrections` regex contract at `src/hooks/use-realtime-voice.ts` reading `/"([^"]+)"\s*→\s*"([^"]+)"\s*\(([^)]+)\)/g` from the assistant's plain-text Correction Report) is superseded by structured Realtime tool-call ingestion. `src/lib/prompts/conversation.ts` `buildConversationPrompt` drops the entire `## Correction Report (Plain Text — Read Aloud)` block (15 lines deleted — Story 10-2 "delete don't alias") and replaces it with a `## Correction Reporting (Tool-Call)` block instructing the model to invoke `report_correction({ original, corrected, explanation, category })` whenever it detects an error in the user's French (mode-agnostic: companion + debate + tcf_simulation all share the same tool-call instruction; the Story 10-7 stale `"Score their argumentation quality in the Correction Report"` debate-mode anchor is rewritten as `"comment on the rhetorical issue naturally in your spoken response"`). The `tools: [...]` array in `useRealtimeVoice`'s `RealtimeConfig` gains a third entry alongside the existing `save_vocabulary` and `note_error_pattern` tools; the new tool's `parameters` JSON schema constrains `category` to the same 4-literal union (`"grammar" | "pronunciation" | "vocabulary" | "register"`) as `Correction["category"]` at `src/types/conversation.ts`. Runtime validation lives in `reportCorrectionArgsSchema` at `src/lib/schemas/ai-responses.ts` (alongside `correctionCategorySchema` exported for future single-source-of-truth use). Pure helpers at `src/lib/realtime-corrections.ts` — `processReportCorrectionCall(parsed: unknown): { outcome: "recorded" | "invalid", ... }` + `drainPendingCorrections(buffer: Correction[]): Correction[]` — own the safeParse and buffer-mutation semantics so the tool-call pipeline can be unit-tested without rendering React. On `response.function_call_arguments.done` for `name === "report_correction"`, `handleFunctionCall` routes args through `processReportCorrectionCall` → on success pushes to the new `pendingToolCorrectionsRef` per-turn buffer + `sendFunctionResult(callId, "Correction recorded.")`; on failure breadcrumbs `feature: "realtime-report-correction"` + ZodIssueCode + `sendFunctionResult(callId, "Invalid correction shape; correction not recorded.")` so the model can self-correct. The buffer is drained by the new `parseCorrections` callback (signature preserved per Story 9-5 contract — pure helper module at `src/lib/realtime-transcript.ts` NOT touched; `text` parameter ignored; `drainPendingCorrections` mutates the array in place and returns its prior contents); buffer is also cleared on `response.done` (with a `"Pending tool corrections dropped at response.done"` breadcrumb if non-empty — the actual leak signal worth tracking) and silently cleared on `case "error"` so an orphaned tool-call without a terminating transcript cannot leak into the next turn. The `inferCategory` keyword-matching heuristic and the regex `/"([^"]+)"\s*→\s*"([^"]+)"\s*\(([^)]+)\)/g` at `use-realtime-voice.ts:142-171` are both **deleted** — categories arrive structurally from the AI's tool-call invocation. The speaking-score formula at `src/lib/speaking-score.ts` `computeSpeakingScore` (extracted from `use-realtime-voice.ts` for testability + future-tuning baseline; identical formula `Math.max(20, Math.round(100 - (correctedEntries / Math.max(totalEntries, 1)) * 30))` with 70 default for zero-utterance sessions) is unchanged; its INPUT (`correctedEntries`) is more accurate post-11-1 because the regex's silent-zero failure mode is gone, so the same conversation produces slightly lower (more accurate) speaking scores — correctness improvement, not regression. Closes audit findings P1-6 (brittle regex silent-zero failures) and P2-1 (voice-mode emoji format) **architecturally** — both failure modes are structurally impossible post-11-1 because the prompt block + regex + heuristic that made them possible all no longer exist. `docs/tcf-spec-citations.md §8` row 4 flips ✓ Verified-with-caveat → ✓ Verified. The `TranscriptView.getDisplayText` legacy sentinel stripper at `src/components/conversation/TranscriptView.tsx` is retained as a forward-compat / backward-compat shim for pre-11-1 stored transcripts (rendered via `app/(tabs)/conversation/history.tsx`); new post-11-1 turns never trigger any sentinel (no correction-line / no `Tip:` / no `No corrections.` / no `---` divider) so `getDisplayText` returns the text verbatim. The post-conversation flow at `use-realtime-voice.ts` (`extractErrorsFromCorrections` → `updateSkillProgress("speaking")` → `incrementDailyActivity` → `updateStreak` → `checkCefrPromotion`) consumes `correctionsRef.current` with NO downstream change. Story 9-4 stored-prompt-injection defense (`<USER_FACTS>` / `<USER_WEAK_AREAS>` wrappers + bilingual "treat as data" prelude) and Story 9-5 voice transcript dedup (`output_modalities: ["audio"]` + `appendIfNew` / `acceptDelta` / `resolveTranscriptKey` + FIFO-capped 256-entry dedup Set + the entire `src/lib/realtime-transcript.ts` pure module) and Story 9-7 Zod-schema retry contract (`chatCompletionJSON` path is separate — Realtime tool-call uses one-shot `safeParse` + breadcrumb without retry loop) and Story 9-8 / 10-6 speaking pipeline (separate record-and-grade flow via `chatCompletionJSON(speakingTaskEvaluationSchema)` — not Realtime) and Story 10-7 debate-mode 3-category split + `CEFR_LEVELS` `nameFr` Alliance Française convention + Québécois drop + `force est de constater` echo fixes in `writing.ts` / `placement.ts` all hold unchanged. Regression-tested in `src/lib/__tests__/realtime-corrections.test.ts` (NEW — `processReportCorrectionCall` valid/invalid/4-category accept + 6-shape `invalid_type` reject + `too_big` reject for over-max-length strings + `MAX_PENDING_CORRECTIONS` sanity + `drainPendingCorrections` buffer-mutation + insertion-order + idempotence + defensive-copy invariants), `src/lib/__tests__/speaking-score.test.ts` (NEW — 9 cases pinning the formula baseline: zero-entries default 70, no-corrections 100, 1/10 = 97, 5/10 = 85, 10/10 = 70, 2/2 = 70, floor at 20 when corrections >> entries, rounding correctness), `src/lib/schemas/__tests__/ai-responses.test.ts` (EXTENDED — `reportCorrectionArgsSchema` cases: well-formed + 4-category accept + invalid-category reject + 3-field empty-string reject + missing-field reject + non-string type reject + bidirectional `ReportCorrectionArgs` ↔ `Correction` structural-compat + order-independent `correctionCategorySchema.options` membership pin + `note_error_pattern` inline-enum-sync lockstep test against `correctionCategorySchema.options`), `src/lib/prompts/__tests__/conversation.test.ts` (EXTENDED — Story 11-1 describe block with 6 levels of tool-call-block-header coverage + 3 modes × multiple drop-the-legacy-Correction-Report cases scoped to the Correction Reporting block via `^…$` anchored regex + multiple-invocations-per-turn assertion scoped to the block slice + Story 10-7 debate-mode + tcf_simulation surfaces still-green regression), and `src/lib/__tests__/realtime-dedup.test.ts` (Case 14 REPLACED — pre-11-1 `parseCorrectionsForTest` regex mirror DELETED; new Case 14 pins the `parseCorrections` callback-invocation-exactly-once-per-unique-key contract via a `jest.fn()` spy, defending against a future refactor that double-invokes the callback and silently drains the buffer twice). Verified 2026-05-11, story 11-1.

**Realtime reconnect + barge-in:** post-Epic-11.2, `src/lib/realtime.ts` `RealtimeSession` gains auto-reconnect on unexpected post-open WebSocket closes (exponential-backoff schedule `[500, 1000, 2000, 4000, 8000]` ms; max 5 attempts ≈ 15.5s total budget; `MAX_RECONNECT_ATTEMPTS === RECONNECT_BACKOFF_MS.length` lockstep pin at `src/lib/realtime-reconnect.ts` exports). Pure helper `shouldReconnect(closeReason, wasConnected, attemptCount)` owns the reconnect-decision logic (`closeReason === "user"` → no reconnect; pre-open close → no reconnect; exhausted attempts → no reconnect; otherwise return delay + 1-indexed attempt number). `RealtimeSession.disconnect({ reason: "user" | "reconnect" })` distinguishes intentional vs. unexpected closes via the `intentionallyDisconnected` class flag; pending reconnect timeouts are cleared via `reconnectTimeoutId` on disconnect to defend against navigate-away races. The `wasConnected` flag (set in `ws.onopen`) lets the helper distinguish pre-open closes (existing reject-the-connect-promise path) from post-open closes (potential reconnect candidate). `attemptReconnect()` runs `establishConnection()` (the refactored shared code path between initial `connect()` and per-attempt reconnect; `connect()` now resets the reconnect state + delegates to `establishConnection()` so a per-disconnect attempt counter doesn't leak across `start()` calls); on success resets `reconnectAttempts = 0` and emits `realtime.reconnected`; on synchronous failure (e.g., offline / refreshSession reject / Edge Function error) manually schedules the next backoff cycle since no `ws.onclose` would fire to drive it naturally; on async failure (post-open close on the new socket) the new `ws.onclose` runs the reconnect-decision helper again with the incremented counter. Defense against intentional-disconnect-during-reconnect: `attemptReconnect()` checks `intentionallyDisconnected` at both entry AND after the `establishConnection()` await (closes the new WebSocket immediately if the user navigated away mid-reconnect, so we don't consume an OpenAI session). The session's `RealtimeConfig` (instructions / voice / turn_detection / audio config / tools — including Story 11-1's three tools `save_vocabulary` / `note_error_pattern` / `report_correction`) is re-sent via `configureSession()` on every reconnect; the prompt is **NOT** re-built (which would re-run `buildConversationPrompt` against potentially-changed `memories` / `errorPatterns` props mid-conversation) — the cached `systemPrompt` from the original `start()` is replayed verbatim, preserving Story 9-4's `<USER_FACTS>` / `<USER_WEAK_AREAS>` wrappers + bilingual "treat as data" prelude by definition. **Server-side conversation context is lost across reconnect** (the model starts fresh after the new `session.update`) — intentional trade-off; replaying prior transcript as `conversation.item.create` events is deferred to a future Epic 11.X follow-up. The 60-minute WebSocket connection limit (per OpenAI Realtime API docs) auto-recovers via the same unexpected-close path. The hook (`src/hooks/use-realtime-voice.ts`) routes the new `realtime.reconnecting` event to (1) drain the Story 11-1 `pendingToolCorrectionsRef` into `correctionsRef` via `mergeOrphanCorrections` (P2/P3 pattern — corrections from before the disconnect are preserved across the cross-session boundary), (2) reset per-turn refs (`inflightItemIdRef` + `responseInFlightRef` + `currentAiTextRef` + `aiSpeakingStartedAtMsRef`), (3) set `state.status: "reconnecting"`, (4) stop the prior `ExpoPlayAudioStream` subscription. On `realtime.reconnected`: set `state.status: "connected"` + re-start audio streaming for the new WebSocket. The new `"reconnecting"` `ConversationState.status` lets the UI render a "Reconnecting..." banner during the window. **Barge-in handling**: pure helper `computeBargeInDirective(state, now)` at `src/lib/realtime-barge-in.ts` decides whether to interrupt the AI based on `state.isAiSpeaking` + `inflightItemId` + `aiSpeakingStartedAtMs`. When user audio is detected via `input_audio_buffer.speech_started` while `state.isAiSpeaking === true`, the hook (1) calls `ExpoPlayAudioStream.stopSound()` to halt local playback, (2) sends `{type: "response.cancel"}` to halt the server-side response generation, (3) sends `{type: "conversation.item.truncate", item_id, content_index: 0, audio_end_ms = Date.now() - aiSpeakingStartedAtMsRef.current}` to synchronize the server's transcript with what was actually played. `audio_end_ms` is clamped to non-negative integer via `Math.max(0, Math.floor(now - startedAt))` (clock-skew defense). If `inflightItemId` or `aiSpeakingStartedAtMs` is null at barge-in time (defensive — early speech_started before first delta), the `response.cancel` still fires but the truncate is skipped with a Sentry breadcrumb noting the missing data. `aiSpeakingStartedAtMsRef` is captured on the first `response.output_audio.delta` of a turn and reset on `response.output_audio.done` / `response.done` / `case "error"` / barge-in fire / reconnect-start / `start()`. The hook mirrors `state.isAiSpeaking` into `isAiSpeakingRef` for handler-time access (same `statusRef` pattern from Story 9-X). `RealtimeSession.sendRaw(event: Record<string, unknown>): void` is the new public method for dispatching arbitrary client events that don't have a typed wrapper (`response.cancel` + `conversation.item.truncate`); existing typed methods (`sendText` / `appendAudio` / `commitAudio` / `clearAudioBuffer` / `sendFunctionResult` / `disconnect`) are unchanged. Sentry breadcrumb keys (`feature: "realtime-reconnect"` / `"realtime-barge-in"` + `attempt` + `category`) are all in the existing `SENTRY_EXTRAS_ALLOWLIST` (Story 9-3) — no allowlist extension. Closes audit P1-7 architecturally. Story 9-3 / 9-4 / 9-5 / 9-6 / 9-7 / 9-8 / 9-10 / 10-2 / 10-3 / 10-4 / 10-5 / 10-7 / 10-8 / 11-1 invariants all hold unchanged. Regression-tested in `src/lib/__tests__/realtime-reconnect.test.ts` (NEW — intentional close + pre-open close + first attempt + each attempt 2-5 backoff value + exhausted + lockstep + canonical schedule pin), `src/lib/__tests__/realtime-barge-in.test.ts` (NEW — not-speaking no-op + full directive + null-item-id partial + null-startedAt partial + clock-skew clamp + same-instant boundary + non-integer-input flooring), and `src/lib/__tests__/realtime-corrections.test.ts` (EXTENDED — reconnect-start orphan-drain regression guard preserves Story 11-1 P2/P3 contract across the new cross-session boundary). **Round-2 review patches** (HIGH × 4 + MED × 4): P21 detach old `ws` event handlers in `establishConnection()` BEFORE creating new socket so a queued late `onclose` from the stale socket can't trigger a second reconnect chain after success; P22 set `isAiSpeakingRef.current` synchronously alongside `setState` in `response.output_audio.delta` / `.done` / barge-in / reconnect-start (the render-time mirror had a stale window between setState enqueue and React commit — barge-in would silently miss the first interrupt); P24 do NOT stop/restart audio subscription on reconnect (the `onAudioStream` callback gates on `sessionRef.current?.isConnected`; the subscription stays alive across reconnect and the gate gives correct semantics for free — fixes the UX regression where mic was muted for the entire reconnect window up to 15.5s); P25 `start()` guards against `statusRef.current === "reconnecting"` (without the guard, a `start()` during reconnect would race the pending setTimeout); P26 `ws.onopen` checks `intentionallyDisconnected` BEFORE `configureSession()` to avoid consuming an OpenAI session if the user disconnected during the reconnect await; P27 `sendRaw` breadcrumbs (`feature: "realtime-sendraw-dropped"`) when the WebSocket is not OPEN at send time so dropped barge-in events are visible in Sentry; P28 the `case "error"` handler suppresses known-benign barge-in race codes (`no_response_to_cancel` / `invalid_truncate_audio` / `item_not_found`) — these fire when `response.cancel` or `conversation.item.truncate` races the server's natural end-of-turn; the conversation continues uninterrupted with an info-level breadcrumb; P30 barge-in branch clears `currentAiTextRef.current` so unplayed transcript text from the cancelled response can't prefix the next turn via `acceptDelta`'s adopt path. Verified 2026-05-11, story 11-2.

**Edge Function upstream timeouts:** post-Epic-11.3, every upstream `fetch()` call in the 3 Edge Functions that talk to OpenAI / Azure Speech (`supabase/functions/ai-proxy/index.ts` × 4 sites: chat + Azure TTS + embedding + Whisper; `supabase/functions/realtime-session/index.ts` × 1 site: Realtime ephemeral token issuance; `supabase/functions/pronunciation-assess/index.ts` × 1 site: Azure pronunciation assessment) routes through the shared helper `fetchWithTimeout(upstream, input, init?, timeoutMs?)` at `supabase/functions/_shared/fetch-with-timeout.ts`. The helper wraps `fetch()` with `AbortSignal.timeout(timeoutMs)`; on timeout it rejects with a typed `UpstreamTimeoutError(upstream, timeoutMs, cause)` (the underlying `DOMException` "TimeoutError" is preserved as `cause`). A companion `withTimeout(label, promise, ms)` helper bounds body-consumption phases (`.arrayBuffer()` / `.text()` / `.json()`) that the `AbortSignal.timeout` does not reliably cover (review-round-1 patch P1) — wired into the Azure TTS audio `arrayBuffer()` read in `ai-proxy/index.ts` and into `parseUpstreamError` body reads in `_shared/errors.ts` (`ERROR_BODY_READ_TIMEOUT_MS = 5_000` — error bodies should be tiny). Per-upstream budgets: `DEFAULT_UPSTREAM_TIMEOUT_MS = 30_000` (chat, embedding, TTS, realtime-token, pronunciation), `WHISPER_UPSTREAM_TIMEOUT_MS = 90_000` (Whisper transcription — bumped from initial 60s per review-round-1 patch D2 to cover the Story 9-8 speaking pipeline 5.5-min AAC scenario; Whisper p99 ≈ ~30s with model-load tail spikes observed up to ~60s; 90s leaves ~30s headroom under the 150s platform kill). New `UPSTREAM_TIMEOUT` `ErrorCode` member + `timeoutResponse(corsHeaders, { upstream, timeoutMs, retryAfter? })` helper at `supabase/functions/_shared/errors.ts` produces a structured 504 response with body `{ error, code: "UPSTREAM_TIMEOUT", retryAfter, upstream, timeoutMs }` (structured `upstream` + `timeoutMs` fields per review-round-1 patch P6; `retryAfter` defaults to 5s but is parameterized per review-round-1 patch D1) and a `Retry-After: 5` header. The `error` field carries the load-bearing message format `"Upstream timeout: {upstream} did not respond within {timeoutMs}ms"` — the literal lowercase substring `"timeout"` is required so the client-side `isRetryable()` regex at `src/lib/openai.ts:23-37` (`/network|timeout|fetch|500|502|503|429|rate limit/`) automatically triggers the existing `MAX_RETRIES = 2` retry-with-backoff path — end-to-end behavior preserved by construction; no client-side code changes. The format is **load-bearing and contractual**: the obvious-natural `"timed out after Xms"` does NOT contain the substring `"timeout"` (it's two words `"timed out"`) and was REJECTED by the RED-phase Jest drift detector. A real-source drift detector at `src/lib/__tests__/upstream-timeout-error.test.ts` (review-round-1 patch P2, replaces the prior inline-mirror approach) reads the Deno helper source from disk and pins the exact format string + the `"timeout"` substring + all constants + the typed-error shape + the absence of the legacy format so any future drift trips CI. Pure helper `isUpstreamTimeoutError(err): err is UpstreamTimeoutError` keeps the Edge Function catch sites `instanceof`-free; it falls back to a `name === "UpstreamTimeoutError"` check (review-round-1 patch P3) so cross-realm / polyfill-substituted error instances still discriminate correctly. A typed `FetchWithTimeoutMisuseError` (with `code: "FETCH_WITH_TIMEOUT_MISUSE"`) is thrown synchronously when a caller passes their own `init.signal` (review-round-1 patch P7; combining signals is out of scope for v1 — a future story can adopt `AbortSignal.any([...])`). Every timeout fire (both fetch-phase and body-read-phase) is logged to Supabase function logs via `console.warn("[upstream-timeout]", ...)` so operators can distinguish hung-upstream from other failures without correlating against HTTP access logs (review-round-1 patch P5). `jest.config.js`'s `testPathIgnorePatterns` is anchored to `<rootDir>/supabase/` (review-round-1 patch P4) so the pattern only excludes the project's Edge Functions directory and doesn't accidentally match other paths containing the substring `"supabase"`. All 6 try/catch blocks at the call sites follow the same shape: timeout → `return timeoutResponse(...)` ; non-timeout error → `throw err` (falls through to the outer `INTERNAL_ERROR` catch at the bottom of each function). Story 11-2 reconnect architecturally benefits: the `realtime-session` fetch inside `establishConnection()` is now bounded to 30s instead of 150s — a hung token-issuance fails fast and reliably. Note the 30s still exceeds Story 11-2's 15.5s `RECONNECT_BACKOFF_MS` total, so the benefit is "fails fast vs platform-kill" not "fits within budget" (review-round-1 patch BS3 corrected the prior overclaim). The 150s Supabase platform-level kill is no longer the dominant timeout for any upstream. End-to-end retry-stacked worst case is ~`(30s × 3) + (1s + 2s backoff) ≈ 93s` — well under 150s. The Supabase JS `functions.invoke` client has NO default client-side timeout (review-round-1 patch BS2 corrected the prior false claim of 60s); a future story could opt in to `options.timeout = 35_000` for an additional safety net. The existing `parseUpstreamError` path is preserved unchanged — it still handles non-timeout upstream failures (5xx response bodies, etc.), now with its body read bounded by `withTimeout`. Sentry telemetry unchanged: client surfaces the new error via the existing `code` allowlist key (Story 9-3) — no allowlist extension. Closes audit P1-9 architecturally. Stories 9-3 / 9-4 / 9-5 / 9-6 / 9-7 / 9-8 / 9-9 / 9-10 / 10-2 / 10-3 / 10-4 / 10-5 / 10-6 / 10-7 / 10-8 / 11-1 / 11-2 invariants all hold unchanged. Regression-tested in `src/lib/__tests__/upstream-timeout-error.test.ts` (NEW — 14 Jest drift-detector cases reading the real Deno source from disk + pinning message format + all 3 constants + type-guard semantics + console.warn presence + `withTimeout` export + typed misuse error + negative guard against the legacy `"timed out after"` format) plus `supabase/functions/_shared/__tests__/fetch-with-timeout_test.ts` (NEW — 7 Deno-runnable cases: timeout-on-hung-upstream + happy-path-with-real-server + caller-signal-rejection-with-typed-FetchWithTimeoutMisuseError + substring-in-real-error + constants tier + withTimeout-timeout + withTimeout-happy-path; manual run via `deno test --allow-net=127.0.0.1`, not CI-wired — Epic 15.3 scope). Verified 2026-05-12, story 11-3 (post-review-round-1 patches HIGH × 2 + MED × 4 + LOW × 2 + BS × 3 + D × 2).

**Postgres-backed rate-limit + per-user daily AI cost ceiling:** post-Epic-11.4, every Edge Function rate-limit check (`ai-proxy`, `realtime-session`, `pronunciation-assess`, `account-delete`, `notification-register`, `send-notifications`) routes through three Postgres RPCs declared in `supabase/migrations/20260512000000_rate_limit_and_cost_ledger.sql`: `check_and_increment_rate_limit(p_user_id, p_key, p_limit, p_window_seconds)` (atomic single-statement UPDATE guarded by `request_count < p_limit` — review-round-1 patch P2 replaced the prior insert+conditional-rollback pattern which had a race where two concurrent over-cap denies could both fire `-1` decrements and re-open a closed window), `check_daily_cost_budget(p_user_id, p_estimated_cents)` (read-only pre-flight against `daily_cost_ledger` + `profiles.daily_ai_cost_cents_limit`, default 100¢ = $1.00; accepts NUMERIC for fractional sub-cent precision), and `record_daily_cost(p_user_id, p_cost_cents)` (post-call atomic increment, also NUMERIC). All 3 functions are `SECURITY DEFINER` + `SET search_path = public` per Story 9-9 hardening, called from the new shared module `supabase/functions/_shared/rate-limit-db.ts` which exposes `checkRateLimit` / `checkDailyCostBudget` / `recordDailyCost` / `rateLimitResponse` / `dailyCostCapResponse` (replaces the deleted in-memory `_shared/rate-limit.ts` — Story 10-2 / 11-3 "delete don't alias" pattern). Cross-isolate-correct because the counter lives in Postgres; cold-start isolates query the same source of truth, closing audit **P1-8** by construction. **Ledger storage uses `NUMERIC(20,6)` for `daily_cost_ledger.total_cost_cents`** (review-round-1 patch P3) so sub-cent contributions (e.g., 0.002¢ embeddings) accumulate accurately — the pre-patch `BIGINT` + `Math.ceil` pattern inflated embedding spend by ~500× and locked out embedding-heavy users (memory writes + fact extraction + error-pattern dedup are all embedding-heavy by design). The user-facing limit `daily_ai_cost_cents_limit` stays `INTEGER` (operator-friendly whole cents); the comparison `(v_total + p_estimated_cents) <= v_limit::numeric` casts for fractional math. Per-user daily AI spend ceiling enforced via pre-flight check on `ai-proxy` (chat / tts / embedding / transcribe) + `realtime-session` + `pronunciation-assess` and post-call cost record from OpenAI `usage` objects (chat / embedding) or pessimistic estimates (TTS / Whisper / Azure speech) — closes the spend-cap portion of audit **P1-10** (the per-call `maxTokens` right-sizing portion stays with Story 11.5). Per-model pricing pinned at `_shared/cost-table.ts` with **REFRESH QUARTERLY** stale-bait flag (last refresh 2026-05-12; next due 2026-08-12) — helpers `estimateChatCostCents` / `actualChatCostCents` / `estimateTtsCostCents` / `estimateWhisperCostCents` / `estimateAzureSpeechCostCents` are the single source of truth for both pre-flight estimation and post-flight recording. New `DAILY_COST_CAP_EXCEEDED` `ErrorCode` member returns HTTP 429 with body `{ error, code, kind: "daily-cost-cap", totalTodayCents, limitCents, retryAfter }` — the `kind` field is the client UI discriminator (separates "wait 30s" per-minute rate-limit from "wait til midnight UTC" daily-cost cap). The error `message` contains the literal substring `"rate limit exhausted"` (review-round-1 patch P1) so the client-side `isRetryable()` regex at `src/lib/openai.ts:23-37` (`/network|timeout|fetch|500|502|503|429|rate limit/`) triggers the existing retry path — the cap is still exhausted on retry so the error surfaces to the user after ~5s wasted backoff, acceptable trade-off vs. silently dropping the first 429 because the client thought it wasn't retryable. **All RPC calls wrapped in `withTimeout(5_000)`** (review-round-1 patch P6) so a hung Postgres releases the Edge Function isolate within 5s instead of holding it for the 150s platform kill — re-uses Story 11-3's `withTimeout` helper from `_shared/fetch-with-timeout.ts`; fail-OPEN on timeout (same policy as on RPC error). **Per-isolate failure counter** (`consecutiveRecordFailures`) in `recordDailyCost` (review-round-1 patch P8) escalates the `console.error` log to `[daily-cost-record-rpc][DEGRADED count=N]` after `RECORD_FAILURE_THRESHOLD = 5` consecutive failures so operators grepping Supabase function logs spot a Postgres outage that's silently letting users spend without the cap meter moving; counter resets on the next successful record. **Fail-OPEN policy** on Postgres errors / timeouts: every RPC wrapper logs to `console.error` and returns `allowed: true` — fail-closed creates self-DoS; operators should treat persistent rate-limit-RPC failures as Sev-1. `send-notifications` cron path uses `CRON_SENTINEL_USER_ID = "00000000-0000-0000-0000-000000000000"` + key `"cron"` (no FK on `rate_limit_counters.user_id` so the sentinel works). Single top-level `"ai-proxy"` rate-limit key covers all 4 sub-actions (chat / tts / embedding / transcribe) matching pre-11-4 30/60s total budget — review-round-1 patch BS1 documented this as a deliberate spec deviation; per-action sub-bucketing would 4× the user's effective request budget which wasn't the audit's intent. Per-action **cost-cap pre/post** still happens inside each switch branch (preserves per-action cost granularity); only the rate-limit counter is shared. Whisper byte-derived minutes estimate uses **1,920,000 bytes/min** (PCM16 16kHz mono worst-case; review-round-1 patch P5) which under-estimates duration for compressed formats (AAC, Opus) but never over-denies legitimate users — pre-patch 240,000 bytes/min was 8× off for iOS PCM16 uploads and locked out iOS Whisper users after a few uploads. **`pg_cron` nightly cleanup** at 02:00 UTC vacuums `rate_limit_counters` rows older than 24h and `daily_cost_ledger` rows older than 30d (REVOKE EXECUTE on `cleanup_stale_rate_limits` from public/anon/authenticated per Story 9-9). `cron.schedule` is wrapped in a defensive `cron.unschedule(...)` block (review-round-1 patch P4) so re-running the migration after `supabase db reset` doesn't fail on the `cron.job.jobname` unique constraint. Story 9-3 Sentry allowlist preserved (`code` was already allowlisted; `"DAILY_COST_CAP_EXCEEDED"` is a short categorical string under the 80-char redaction threshold). Story 11-3 invariants hold by construction: the rate-limit-RPC runs UPSTREAM of `fetchWithTimeout` — a budget-exhausted request never reaches the upstream timeout path. Story 11-2 reconnect interaction: a daily-cap-exhausted user attempting a new Realtime session sees a 429 from `realtime-session` which surfaces via `isRetryable("rate limit") === true` → existing client retry → after retries exhaust, the reconnect-failed terminal path runs. Correct UX (budget-exhausted users should not be able to start a fresh Realtime session). **Quarterly refresh discipline**: the `MODEL_RATES` table is stale-bait — operators must update rates quarterly by visiting `openai.com/api/pricing/` + `azure.microsoft.com/en-us/pricing/details/cognitive-services/speech-services/`. Stale rates under-count (we pay more than we record) so the daily cap fires LATER than it should — a soft failure mode. Closes audit P1-8 architecturally + spend-cap portion of P1-10. Stories 9-3 / 9-4 / 9-5 / 9-6 / 9-7 / 9-8 / 9-9 / 9-10 / 10-2 / 10-3 / 10-4 / 10-5 / 10-6 / 10-7 / 10-8 / 11-1 / 11-2 / 11-3 invariants all hold unchanged. Regression-tested in `src/lib/__tests__/cost-table.test.ts` (NEW — 16 Jest drift-detector cases pinning model rates + REFRESH QUARTERLY flag + math contract via mirrored helpers) + `src/lib/__tests__/rate-limit-db.test.ts` (NEW — 17 Jest drift-detector cases pinning the 5 exports + fail-OPEN catch-block invariants + `kind: "daily-cost-cap"` body discriminator + `Math.max(0, ...)` non-negative clamp + P1 message-substring + P3 no-Math.ceil + P6 withTimeout wrapping + P8 DEGRADED escalation) + `supabase/migrations/__tests__/rate_limit_test.sql` (NEW — 8 manual-run pgTAP-style assertions covering allow/deny atomic boundary + per-user cost override + fractional accumulation (0.002 × 100 = 0.2¢) + nightly cleanup + cron-sentinel flow; run via `psql -f`, NOT CI-wired — Epic 15.3 scope). Verified 2026-05-12, story 11-4 (post-review-round-1 patches HIGH × 4 + MED × 3 + LOW × 1 + BS × 1).

**Cost discipline pass (per-call right-sizing + 3→1 post-conv consolidation + gpt-realtime-mini):** post-Epic-11.5, the remaining portions of audit **P1-10** close (Story 11-4 closed the spend-cap portion; 11-5 closes per-call maxTokens + 3-call collapse + free-tier model). **Right-sized `maxTokens` audit**: `src/lib/openai.ts` `chatCompletion` default dropped from 2048 → **800** (sentinel — every call site should override; small default surfaces mis-sized calls via Zod truncation → schema parse failure → Sentry); `src/lib/translation-generation.ts` generation 2048 → **1200**, evaluation 2048 → **800**; `src/lib/echo-generation.ts` 2048 → **1200**; `speaking-evaluator.ts` (1024) + `error-tracker.ts` micro-drill (1024) + `error-tracker.ts` batch (1024) + mock-test screen (4096) + placement-test screen (4096) all kept (well-sized per the audit table). A new drift detector at `src/lib/__tests__/maxtokens-audit.test.ts` reads each call-site file from disk and pins the post-11-5 values via regex — catches a future regression to 2048 without thinking about cost. **Consolidated post-conversation analysis** at `src/lib/post-conversation-analysis.ts` (NEW) replaces the pre-11-5 3-call pipeline in `use-realtime-voice.ts`: a single `chatCompletionJSON` call against the new `postConversationAnalysisSchema` (in `src/lib/schemas/ai-responses.ts`) returns `{ facts, errorPatterns, feedback }` in one pass at `maxTokens: 1500` (feedback ~150 + facts ~500 + errorPatterns ~500 + envelope = ~1200; 300 headroom). Sub-arrays use `.default([])` and feedback uses `.optional()` so partial-result tolerance preserves pre-11-5 fire-and-forget semantics — schema-parse retry (Story 9-7's `parseRetries: 1`) only fires on top-level structural mismatch. The new `extractPostConversationAnalysis(args)` + `persistPostConversationAnalysis(args)` pair handles the AI call + 3-way parallel persist (memories + error patterns + `conversations.update({ai_feedback})`) via `Promise.allSettled` — per-slot failures route through `captureError(_, "post-conversation-persist")` without blocking the other slots. New shared `persistMemories(userId, conversationId, facts[])` (extracted from `memory.ts`) and `persistErrorPatterns(userId, patterns[])` (extracted from `error-tracker.ts`) are the new write APIs. **The pre-11-5 `extractAndStoreMemories` is DELETED** (Story 10-2 / 11-3 / 11-4 "delete don't alias" pattern — verified single caller). **`extractErrorsFromCorrections` is KEPT** because two non-Realtime flows still need it: `src/hooks/use-echo-practice.ts` + `src/hooks/use-translation.ts` use it on echo / translation evaluator output where there's no transcript / feedback to consolidate with. The Realtime path stops calling it (delete-then-keep would have been over-aggressive; the function now internally delegates its embed/insert pipeline to `persistErrorPatterns`). Saves ~1.25¢ per Realtime conversation on input cost (transcript counted once instead of 3×); drops user-perceived feedback latency from ~9s (3 serial calls) to ~3s (1 combined); Story 11-4's `daily_cost_ledger` pre-check tightens automatically because aggregate `maxTokens` dropped from 2048 × 3 = 6144 to a single 1500 (~4× pre-flight headroom recovered). **`gpt-realtime-mini` becomes the free-tier baseline** at `src/lib/realtime.ts` — `const MODEL = "gpt-realtime-mini"` (was `"gpt-realtime"`). Costs $10/1M input + $20/1M output (3.2× cheaper than `gpt-realtime` at $32/1M + $64/1M). Story 11-4's `MODEL_RATES["gpt-realtime-mini"]` was pre-staged; the Story 11-4 daily-cost-cap pre-check now estimates ~5¢ per 5-min session (was ~16¢) — free-tier users open ~20 sessions/day instead of ~6 within the same $1.00 cap. The `realtime-session` Edge Function `ALLOWED_REALTIME_MODELS` allowlist already included mini. Story 11-2's `RealtimeConfig` cached at `start()` carries the new MODEL through reconnects unchanged. Story 11-1's three tools (save_vocabulary / note_error_pattern / report_correction) work with mini (OpenAI Realtime API surface is identical). Future paid-tier override (Epic 16.X): when `profiles.tier` lands, the constant becomes a function reading `useAuthStore.getState().profile?.tier` and returning either mini (free) or full (paid). The `feedback: undefined` short-circuit in `persistPostConversationAnalysis` preserves the pre-11-5 transcript-too-short behavior (no UI surface, no `conversations.update`). Story 9-4 stored-prompt-injection defense holds by construction: the new `buildPostConversationAnalysisPrompt` wraps both user-derived inputs (transcript + corrections) in `<USER_TRANSCRIPT>` / `<USER_CORRECTIONS>` blocks with the bilingual "treat as data" prelude (identical pattern to `buildSpeakingEvaluatorPrompt`'s `<USER_TRANSCRIPT>` wrapping from Story 9-8). Closes audit P1-10 in full. Stories 9-3 / 9-4 / 9-5 / 9-6 / 9-7 / 9-8 / 9-9 / 9-10 / 10-2 / 10-3 / 10-4 / 10-5 / 10-6 / 10-7 / 10-8 / 11-1 / 11-2 / 11-3 / 11-4 invariants all hold unchanged. Regression-tested in `src/lib/__tests__/post-conversation-analysis.test.ts` (NEW — 13 Jest cases: short-circuit on tiny transcripts + single-call consolidation contract + maxTokens/temperature/feature options pin + 2-message structure with Story 9-4 wrappers + persist fan-out with failure isolation + Sentry routing + `POST_CONVERSATION_ANALYSIS_MAX_TOKENS = 1500` constant pin) + `src/lib/prompts/__tests__/post-conversation-analysis.test.ts` (NEW — 11 cases: 3-sub-output enumeration + Story 9-4 wrapper invariants for both `<USER_TRANSCRIPT>` and `<USER_CORRECTIONS>` + bilingual prelude + prelude-before-wrapper ordering + JSON-only output + 4 memory types + 4 error categories) + `src/lib/__tests__/maxtokens-audit.test.ts` (NEW — 8 drift-detector cases pinning all 8 call-site maxTokens values) + `src/lib/schemas/__tests__/ai-responses.test.ts` (EXTENDED — 8 new `postConversationAnalysisSchema` cases: well-formed + partial defaults + invalid category/type/rating rejection). **Story 11-5 review-round-1 patches** (HIGH × 4 + MED × 4 + LOW × 2): P1 restored per-fact content safety rules to the consolidated system prompt (the deleted `extractAndStoreMemories` had explicit imperative-blocklist + URL/markup-blocklist + "DROP THAT FACT ENTIRELY" rules that defended at the extractor layer; the new consolidated prompt drops those by mistake, weakening Story 9-4's defense leg-1 — port restored + 3 regression tests added); P2 silent-empty-result detection — when transcript ≥ 50 chars but the model returns `{}` (all `.default([])` and `.optional()` schemas accept it without firing `parseRetries: 1`), the extractor now emits `addBreadcrumb({level: "warning", feature: "post-conversation-analysis-empty"})` so operators can spot a model silently producing no useful output; P3 fulfilled-but-errored Supabase slot detection in `Promise.allSettled` — supabase-js v2 query builders resolve with `{ data, error }` and NEVER reject on Postgres-side errors (RLS denial, FK violation, PostgrestError); the pre-patch loop only inspected `status === "rejected"` so feedback could show in UI while the DB never persisted; post-patch we also inspect `result.value?.error` on fulfilled supabase slots; P4 `safeStringifyCorrections` switched from a byte-level cap (4000 chars with `" /* truncated */]"` marker that produced malformed JSON inside `<USER_CORRECTIONS>`) to an element-level cap (`corrections.slice(0, MAX_CORRECTIONS_ELEMENTS = 50)` — always produces valid JSON, matches Story 11-1's `MAX_PENDING_CORRECTIONS = 50` cap); P5 short-transcript path now still persists error patterns when `corrections.length > 0` — pre-11-5 `extractErrorsFromCorrections` had its own corrections-only gate; post-11-5-pre-patch the consolidated gate dropped all 3 outputs on short transcripts, losing error tracking for 2-3-turn debug sessions; post-patch a fallback branch maps `Correction[]` → `persistErrorPatterns` directly bypassing the AI enrichment step; P6 schema `.max(10)` on `facts` + `errorPatterns` arrays — without the cap a miscalibrated model could return 50+ facts kicking off 50+ concurrent `generateEmbedding` calls and hitting Story 11-4's shared `"ai-proxy"` rate-limit; P7 server-side `ai-proxy` `chatMaxOutput` + `max_completion_tokens` defaults dropped from 2048 → 800 to match the client `chatCompletion` default (no drift across the boundary); P8 `result as PostConversationAnalysis` cast replaced with defensive defaults `{facts: Array.isArray(...) ? ... : [], errorPatterns: ..., feedback: ... ?? undefined}` so an unexpected `null` from a future transform doesn't `.map()`-TypeError downstream; P9 `persistErrorPatterns` filter-drops now emit Sentry breadcrumbs (`feature: "error-pattern-category-drop"` / `"error-pattern-pattern-drop"`) so operators can grep for systemic category-typo issues from the echo + translation evaluator paths; P10 new cost-table drift detector pins that the realtime `MODEL` constant has a corresponding `MODEL_RATES` entry in cost-table.ts (catches a future cost-table refresh that accidentally drops `"gpt-realtime-mini"` causing silent fall-through to `gpt-4o` rates in the daily-cost-cap pre-check). +13 net Jest cases (1067 → 1080). Verified 2026-05-13, story 11-5 (post-review-round-1 patches HIGH × 4 + MED × 4 + LOW × 2).

**Embedding-based dedupe in error-tracker:** post-Epic-11.6, audit finding **P1-21** closes architecturally. Pre-11-6, `trackError` at `src/lib/error-tracker.ts:59-66` used byte-exact string-equality on `error_description` (`.eq("error_description", safeDescription)`) — so the AI-emitted variants "Confuses passé composé with imparfait" / "Mixes passé composé and imparfait" / "Uses passé composé where imparfait is needed" produced three separate `error_patterns` rows with `occurrences = 1` each instead of a single consolidated row, defeating `MICRO_DRILL_THRESHOLD = 3` (the targeted-micro-drill engine never fired for semantically-clustered errors). Forward-only schema migration `supabase/migrations/20260513000000_error_patterns_embedding.sql` adds `embedding VECTOR(1536)` to `error_patterns` + `idx_error_patterns_embedding` HNSW index (mirrors `companion_memory`'s pattern from `20260301000002`) + new `match_error_pattern(p_error_type, p_error_description, p_query_embedding, p_threshold DEFAULT 0.85)` RPC with `SECURITY DEFINER` + `SET search_path = public` + `auth.uid()` filter (Story 9-9 hardening). The RPC's `WHERE` clause is **hybrid**: Arm 1 matches new rows with `embedding IS NOT NULL AND 1 - (ep.embedding <=> p_query_embedding) > p_threshold` (cosine STRICT greater-than; at exact 0.85 → NO match — defends against false-positive merges at the boundary), Arm 2 matches legacy NULL-embedding rows with `embedding IS NULL AND error_description = p_error_description` (string-equality fallback) — so existing pre-11-6 data continues to dedupe without a one-shot backfill script. `ORDER BY similarity DESC LIMIT 1` returns the single best match. `trackError(userId, errorType, description)` refactored to: (1) `sanitizeMemoryContent` description (preserves Story 9-4 invariant — embedding represents sanitized stored text); (2) `generateEmbedding(safeDescription)` from `src/lib/openai.ts:372` — load-bearing ordering, same pattern as `persistMemories` from Story 11-5; (3) call `match_error_pattern` RPC; (4) on match → UPDATE `occurrences + 1` + `last_occurred`; on no match → INSERT new row including `embedding: JSON.stringify(queryEmbedding)`. **Fail-OPEN policy**: `generateEmbedding` rejection (network, daily-cost-cap exhausted, API 5xx) routes through `captureError(_, "track-error-embedding")` and falls back to pre-11-6 string-equality dedup via `.eq("error_description", _).maybeSingle()` — same fail-OPEN policy as Story 11-4's `checkRateLimit` Postgres-error path. INSERT payload conditionally includes the `embedding` key (ABSENT when fallback ran without embedding — keeps Supabase happy + lets the row participate as legacy via Arm 2 on future writes). `ERROR_PATTERN_SIMILARITY_THRESHOLD = 0.85` exported from `error-tracker.ts` for client-side test pinning. New `feature` Sentry tags: `track-error-embedding` / `track-error-rpc` / `track-error-fallback` — all short categorical strings under Story 9-3's 80-char redaction threshold; `feature` and `errorType` already allowlisted (no extension needed). **No backfill** of legacy NULL-embedding rows is performed — operator-decision rationale: running `generateEmbedding` across every existing row is unbounded per-user cost; legacy rows participate via Arm 2 and converge to resolved=TRUE over time (future story can opportunistically backfill on Arm-2 match). Boundary tests pin both 0.85-exact-no-match AND 0.851-match. The canonical P1-21 closure proof: 4 calls to `trackError` with 4 near-duplicate `passé composé / imparfait` descriptions produce a SINGLE row with `occurrences = 4` (preserving the first-inserted description; verified via end-to-end simulator with in-memory cosine math). Per-call cost addition: ~120 tokens × $0.02/1M = $0.0000024 per `trackError` invocation (Story 11-4's `cost-table.ts` `MODEL_RATES["text-embedding-3-small"]` already pinned; `daily_cost_ledger` already tracks the embedding action arm at `ai-proxy`). The `note_error_pattern` Realtime tool-call dispatch at `use-realtime-voice.ts:318-323` calls `trackError(user.id, parsed.error_type, parsed.description)` unchanged — dedup happens BEHIND the boundary. `persistErrorPatterns` (Story 11-5) calls `trackError` per item at line 263 unchanged. `extractErrorsFromCorrections` (echo + translation flows, Story 11-5 kept) calls `persistErrorPatterns` unchanged. Single entry point at `trackError`; no call-site changes outside `error-tracker.ts` + the migration. Closes audit P1-21 architecturally. Stories 9-3 / 9-4 / 9-5 / 9-6 / 9-7 / 9-8 / 9-9 / 9-10 / 10-2 / 10-3 / 10-4 / 10-5 / 10-6 / 10-7 / 10-8 / 11-1 / 11-2 / 11-3 / 11-4 / 11-5 invariants all hold unchanged. Regression-tested in `src/lib/__tests__/error-tracker-dedupe.test.ts` (NEW — 14 Jest cases: threshold pin + RPC-match-UPDATE happy path + RPC-empty-fallback-INSERT-with-embedding happy path + 3 fail-OPEN paths each with Sentry routing + UPDATE-error + INSERT-error + 3 short-circuit guards + sanitize-before-embed invariant + call-count contracts), `src/lib/__tests__/error-tracker-e2e.test.ts` (NEW — 4 cases: the canonical 4-variant `passé composé / imparfait` P1-21 closure proof with in-memory cosine simulator + 0.85-exact-no-match boundary + 0.851-match boundary + legacy NULL-embedding string-equality Arm-2 fallback), `src/lib/__tests__/error-patterns-migration-drift.test.ts` (NEW — 10 drift-detector cases reading the SQL migration from disk: column shape + HNSW index + SECURITY DEFINER + search*path + 0.85-threshold pin + strict-greater-than-not-gte + auth.uid()-not-param + both Arm 1 + Arm 2 WHERE clauses present + DROP-FUNCTION-IF-EXISTS-signature + resolved-FALSE + RETURNS-TABLE-shape) + `supabase/migrations/__tests__/match_error_pattern_test.sql` (NEW — 7 manual-run pgTAP-style assertions: function-hardening + empty-case + Arm-1-match + 0.85-boundary-exclusion + Arm-2-string-equality + resolved-exclusion + cross-user-isolation; run via `psql -f`, NOT CI-wired — Epic 15.3 scope). Verified 2026-05-13, story 11-6. **Story 11-6 review-round-1 patches** (BS × 1 + HIGH × 3 + MED × 5 + LOW × 7): BS1 spec line 11 boundary phrasing typo fixed ("exact 0.85 is NOT a match"); P1 new `isValidEmbedding(vec): vec is number[]` helper + `EMBEDDING_DIMENSION = 1536` constant in `error-tracker.ts` guards against `generateEmbedding` returning empty `[]` or non-1536-dim arrays (a future `dimensions` API override would have failed the Postgres VECTOR cast at the RPC); P2 same helper checks `Number.isFinite(x)` for every component to reject NaN / Infinity (would've serialized to JSON `null` and failed cast); P3 pgTAP test JWT setting key fix — `set_config('request.jwt.claims', json_build_object('sub', uuid)::text, true)` (the pre-patch `request.jwt.claim.sub` key didn't populate `auth.uid()`, leaving test 7 cross-user-isolation passing vacuously); P4 top-level `try/catch` in `trackError` routing unexpected throws to `captureError(*, "track-error-unexpected")`so a sanitizer regex engine exception can't abort the`persistErrorPatterns` loop mid-batch; P5 RPC row + fallback row shape guards (`typeof row.id === "string" && typeof row.occurrences === "number"`) — pre-patch a malformed row would `existing.occurrences + 1`→ NaN UPDATE; P6 fail-OPEN routes (embedding/RPC/fallback) switched from`captureError`(error tier) to`addBreadcrumb({ level: "warning", category: "ai", data: { feature, errorType, description } })`— matches Story 11-4`checkRateLimit`and Story 11-5 review P9 fail-OPEN policy (reserve error tier for write failures + the P4 catch-all); P7`description` field included in all 5 Sentry routes consistently (story 9-3 allowlist already permits it); P8 ORDER BY tiebreaker (`similarity DESC, ep.last_occurred DESC, ep.id`) on the RPC prevents non-deterministic Arm-2 legacy-row ties at similarity=1.0; P9 pgTAP test 7 now asserts the returned row's `user_id`ownership (catches the inverted-isolation false-positive case); P10 defensive`CREATE EXTENSION IF NOT EXISTS vector;`prepended to migration so it self-contains its idempotency contract on a stripped DB; P11 dedupe test asserts all 4 fallback`.eq()`filter pairs via the captured`selectEqMock.mock.calls`(prevents regression that drops`.eq("resolved", false)`); P12 absorbed into P6 (breadcrumb routing avoids the PostgrestError-not-Error-instance shape issue); P13 story-file `Array.isArray(data)`doc drift fix (3 occurrences); P14`themedEmbedding`1e-3 perturbation replaced with`variantEmbedding(variant)`using axis-and-angle vectors at cosines`[1.000, 0.955, 0.921, 0.878]`vs the reference — variant 3 is barely above the 0.85 threshold so a future threshold drift to 0.88 would break the proof; P15 migration comment notes future`CONCURRENTLY` requirement on populated-table re-creates. +13 net Jest cases (1108 → 1121). Verified 2026-05-13, story 11-6 (post-review-round-1 patches BS × 1 + HIGH × 3 + MED × 5 + LOW × 7).

**Prompt truncation — top-3 memories + top-3 error patterns + max 80 chars each:** post-Epic-11.7, audit finding **P2-9** closes architecturally. Pre-11-7 `buildConversationPrompt` at `src/lib/prompts/conversation.ts:23` had a single shared `MAX_PROMPT_USER_ITEMS = 20` count cap and NO per-item char cap — so a long-tenure user with 20 memories at 300 chars each + 20 error patterns at 300 chars each pushed ~12,000 chars (~3,000 tokens) of user-derived content into the Realtime system prompt on top of the ~2,500-token base prompt. At `gpt-realtime-mini` rates (Story 11-5) that added ~$0.00003/turn × 30-60 turns/session = ~$0.001-$0.0018/session in prompt-tax, AND ~144-288ms of TTFT (time-to-first-token) prefill latency at the front of every session + every Story 11-2 reconnect (cached `RealtimeConfig.systemPrompt` is replayed verbatim on reconnect). Three new exported constants in `conversation.ts` enforce the bound: `MAX_PROMPT_MEMORIES = 3` + `MAX_PROMPT_ERROR_PATTERNS = 3` (per-block count caps, replacing the deleted shared `MAX_PROMPT_USER_ITEMS`) + `MAX_PROMPT_ITEM_CHARS = 80` (per-item byte cap, PROMPT-INJECTION-ONLY — does NOT affect `sanitizeMemoryContent`'s `MAX_MEMORY_CHARS = 300` storage cap at the write boundary). New pure helper `truncateToBytes(text, max)` exported from the same module: predictable hard slice (no ellipsis, no word-boundary heuristics) with two defensive guards mirroring `sanitizeMemoryContent`'s logic at `src/lib/memory.ts:175-181` — (1) surrogate-pair backoff (cut that would split a UTF-16 high surrogate like an emoji 🎉 backs off by 1 to avoid emitting a lone surrogate that breaks JSON/UTF-8), (2) partial-marker tail strip (cut landing inside a `[redacted:...]` injection-redaction marker strips the partial fragment + trimEnd). The two injection blocks (`<USER_FACTS>` + `<USER_WEAK_AREAS>`) now use the pipeline `sanitize → filter-empty → slice(N) → truncate → filter-empty`: sanitize-FIRST preserves the Story 9-4 stored-prompt-injection invariant (injection tokens stripped before any other transformation operates on the string); slice-BEFORE-truncate avoids wasted work on items we'd discard; the second filter-empty defends against the degenerate truncate-to-empty edge case. The Realtime session bootstrap at `app/(tabs)/conversation/[sessionId].tsx:205-206` is reduced from `retrieveMemories(user.id, topic, 8) + getTopErrors(user.id, 5)` (13 rows fetched but only 13/20 injected — wasted Supabase + pgvector cosine RPC work on rows the builder accepted anyway) to `retrieveMemories(user.id, topic, MAX_PROMPT_MEMORIES) + getTopErrors(user.id, MAX_PROMPT_ERROR_PATTERNS)` (3 + 3 = 6 rows fetched, 6 injected — bounded I/O matches bounded prompt). `MAX_PROMPT_USER_ITEMS` is **DELETED** (Story 10-2 / 11-3 / 11-4 / 11-5 / 11-6 "delete don't alias" pattern; verified single in-file caller via `grep -rn "MAX_PROMPT_USER_ITEMS"`); a star-import test in `conversation-truncation.test.ts` asserts the export is `undefined` so a future refactor that re-introduces the constant fails CI loudly. **`use-daily-briefing.ts` is explicitly NOT touched** — daily briefing renders memories + top errors to a non-Realtime UI surface (no token tax), and applying the 80-char per-item cap there would degrade user-visible text. The user-derived prompt tail drops from worst-case ~12,000 chars (~3,000 tokens) → bounded ~480 chars (~120 tokens) — a **25× reduction on the user-derived tail** with **estimated savings** of ~144-288ms TTFT per session + ~$0.001-$0.0018 cost/session at gpt-realtime-mini rates (worst-case basis: long-tenure user with 20+ memories at max storage size; the broader prompt-size delta is smaller because the ~2,500-token base prompt + Story 9-4 bilingual "treat as data" prelude + Story 11-1 tool-call definitions are unchanged; no telemetry yet validates the empirical savings). Story 11-2 reconnect path inherits the smaller prompt for free (cached `RealtimeConfig.systemPrompt` is replayed verbatim); Story 11-4's `daily_cost_ledger` pre-flight check at `realtime-session/index.ts` tightens automatically because the per-call `estimateChatCostCents` input-token estimate is lower. Story 11-6's `match_error_pattern` RPC + `embedding VECTOR(1536)` column + HNSW index + `isValidEmbedding` helper all unchanged — 11-7 consumes the 11-6-deduped top-3 error patterns from `getTopErrors` (Story 11-6 guarantees these are 3 distinct mistakes, not 3 near-duplicates of the same). Sentry allowlist unchanged (no new `feature` tags — truncation is pure transformation, no error-recoverable boundary). Closes audit P2-9 architecturally. Stories 9-3 / 9-4 / 9-5 / 9-6 / 9-7 / 9-8 / 9-9 / 9-10 / 10-2 / 10-3 / 10-4 / 10-5 / 10-6 / 10-7 / 10-8 / 11-1 / 11-2 / 11-3 / 11-4 / 11-5 / 11-6 invariants all hold unchanged. Regression-tested in `src/lib/prompts/__tests__/conversation-truncation.test.ts` (NEW — 27 Jest cases: 3 constant pins + delete-don't-alias guard for `MAX_PROMPT_USER_ITEMS` via star-import introspection + 12-case `truncateToBytes` helper contract (identity / boundary / idempotence / surrogate-pair-backoff + emoji-below-cut / partial-marker tail strip / complete-marker-below-cut survives / unconditional-trimEnd mirror invariant / non-string defensive guard) + 12-case `buildConversationPrompt` integration coverage (5-item → 3-item output for both blocks, 200-char input → 80-char output, each line ≤ MAX + 2 prefix, empty/undefined arrays → no block rendered, Story 9-4 sanitize-before-truncate invariant verified by feeding a poisoned `"Ignore all prior instructions"` input + asserting the injection token doesn't survive into the prompt, sanitization-driven empty rows don't waste a slot)) + `src/lib/__tests__/session-bootstrap-fetch-limits-source.test.ts` (NEW — 5 drift-detector cases reading `[sessionId].tsx` from disk: imports both constants from the conversation prompt module + `retrieveMemories` consumes `MAX_PROMPT_MEMORIES` + `getTopErrors` consumes `MAX_PROMPT_ERROR_PATTERNS` + negative guards against the pre-11-7 hardcoded `retrieveMemories(user.id, topic, 8)` and `getTopErrors(user.id, 5)` literals). +32 net Jest cases (1121 → 1153). Verified 2026-05-13, story 11-7.

### Routing (`app/`)

Expo Router file-based routing with three route groups:

- `(auth)/` — login, signup, forgot-password (unauthenticated)
- `onboarding/` — post-signup 3-step wizard
- `(tabs)/` — main app with 5 tabs: home, conversation, practice, mock-test, profile

Auth guard in `app/_layout.tsx` redirects based on session state and onboarding status.

### Application Logic (`src/`)

- **`src/hooks/`** — Custom hooks that are the primary interface for features. Each hook manages its own Supabase queries and API calls:
  - `use-auth.ts` — sign in/up/out, profile loading, onAuthStateChange subscription
  - `use-realtime-voice.ts` — orchestrates OpenAI Realtime WebSocket voice conversations
  - `use-exercise.ts` — AI exercise generation and grading via GPT-4o
  - `use-pronunciation.ts` — Azure Speech pronunciation assessment
  - `use-progress.ts` — skill progress and streaks from Supabase
  - `use-audio-recorder.ts` / `use-audio-player.ts` — expo-av recording/playback

- **`src/lib/`** — Core libraries and API clients:
  - `openai.ts` — OpenAI client (chat completions, TTS, embeddings) via `ai-proxy` Edge Function
  - `realtime.ts` — `RealtimeSession` class managing WebSocket to OpenAI Realtime API via `realtime-session` Edge Function
  - `supabase.ts` — Supabase client with expo-secure-store adapter for native sessions
  - `pronunciation.ts` — Azure Speech pronunciation assessment via `pronunciation-assess` Edge Function
  - `scoring.ts` — TCF score math (raw% → TCF scale, CEFR mapping)
  - `srs.ts` — SM-2 spaced repetition algorithm for vocabulary
  - `memory.ts` — Vector-based companion memory (extract facts, embed, store, retrieve via pgvector)
  - `error-tracker.ts` — recurring error pattern tracking and micro-drill generation
  - `prompts/` — System prompt builders per feature (conversation, grammar, listening, reading, writing, mock-test)

- **`src/store/`** — Zustand stores:
  - `auth-store.ts` — session, user, profile, isOnboarded

- **`src/types/`** — TypeScript types for CEFR/TCF domain, conversations, exercises, user profile

- **`src/components/`** — Reusable UI components organized by feature (conversation/, practice/)

### Database (`supabase/migrations/`)

- `001_initial_schema.sql` — All tables, RLS policies, auth trigger
- `002_memory_functions.sql` — `match_memories()` pgvector RPC function

Tables include: profiles, skill_progress, conversations, conversation_messages, exercises, vocabulary, mock_tests, mock_test_answers, daily_activity, companion_memory, error_patterns. All have RLS enforcing `auth.uid() = user_id`.

### Styling

NativeWind v4 (Tailwind for React Native). Custom theme in `tailwind.config.js`:

- `primary`: navy blue (`#1E3A5F`)
- `accent`: amber/gold (`#F5A623`)
- `success`: `#34C759`, `error`: `#FF3B30`
- `surface`: off-white (`#F5F5F0`)

Global CSS at `src/styles/global.css`.

## Key Conventions

- **Path alias**: `@/*` maps to repo root (e.g., `import { supabase } from '@/src/lib/supabase'`)
- **TypeScript strict mode** enabled
- **Typed routes** enabled (`experiments.typedRoutes` in app.json)
- All AI API calls are proxied through Supabase Edge Functions (`ai-proxy`, `realtime-session`, `pronunciation-assess`) — API keys never leave the server
- All Supabase tables use Row-Level Security scoped to `auth.uid()`
- The `components/` directory at repo root contains unused Expo boilerplate — new components go in `src/components/`
